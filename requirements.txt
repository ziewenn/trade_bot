# Core
websockets>=12.0
aiohttp>=3.9
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0

# Polymarket SDK
py-clob-client>=0.34.6

# Data
aiosqlite>=0.19
scipy>=1.12
numpy>=1.26

# Monitoring
rich>=13.7
structlog>=24.0

# HTTP client (connection pooling for live trading)
httpx[http2]>=0.27

# Web dashboard
fastapi>=0.109
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.6

# Anchor scraping via JS rendering (set ANCHOR_SCRAPE_PLAYWRIGHT_FALLBACK=true in .env)
# Browser: run `playwright install chromium` after pip install
playwright>=1.40

# Testing
pytest>=8.0
pytest-asyncio>=0.23
